#!/bin/bash

# Check if a file path is provided
if [ -z "$1" ]; then
  echo "Usage: $0 path/to/specific_file"
  exit 1
fi

# Commit the specific file directly with a standard commit message
git add "$1" && git commit -m "Updated baseline"

echo "Committed $1 with message 'Updated baseline'"
